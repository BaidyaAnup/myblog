<h1>Pages</h1>

<ul>
  <% @pages.each do |page| %>
    <li>
      <%= link_to page.contact, page %>
      <%= link_to "delete", page_path(page), data:{ method: :delete, confirm:"Are you sure?"} %>

    </li>
  <% end %>
</ul>

<%= link_to "new page", new_page_path %>